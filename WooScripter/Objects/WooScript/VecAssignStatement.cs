using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace WooScripter.Objects.WooScript
{
/*    class VecAssignStatement : Statement
    {
        VecVariable _VecTarget;
        VecExpression _VecExpression;
        VecOp _VecOperator;

        public void Parse(ref string[] program)
        {
            string token = ParseUtils.GetToken(ref program);
            _VecTarget = WooScript.GetVecVar(token);
            _VecOperator = WooScript.GetVecOperator(token);
            _VecExpression = new VecExpression(program);

        }
        public void Execute(ref WooState state)
        {
            _VecOperator.Execute(state, _VecTarget, _VecExpression);
        }
    }*/
}
